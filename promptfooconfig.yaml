description: "ShuTong Eval Pipeline MVP - Fact Retrieval"

prompts:
  - file://evals/wrappers/fact_prompt_wrapper.ts

providers:
  - id: openai:chat:deepseek-ai/DeepSeek-V3
    label: "DeepSeek V3 (Analysis)"
    config:
      apiBaseUrl: ${SILICONFLOW_BASE_URL}
      apiKey: ${SILICONFLOW_API_KEY}
  - id: openai:chat:deepseek-ai/DeepSeek-V3
    label: "DeepSeek V3 (Baseline)"
    config:
      apiBaseUrl: ${SILICONFLOW_BASE_URL}
      apiKey: ${SILICONFLOW_API_KEY}

defaultTest:
  assert:
    - type: javascript
      value: file://evals/evaluators/fact-judge.ts

tests: file://evals/datasets/fact_retrieval.csv
